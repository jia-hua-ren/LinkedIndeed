{
  "name": "job-saver-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Linked, Indeed! browser extension",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^30.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
